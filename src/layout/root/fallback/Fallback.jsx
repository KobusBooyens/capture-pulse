import React from "react";
import Typography from "../../../components/Typography/Typography.jsx";

const Fallback = () => {
    return (
        <Typography>Fallback component</Typography>
    );
};
export default Fallback;
